version: '3.8'

services:
  fraudbusters_phase3a:
    build: .
    ports:
      - "8545:8545"
    network_mode: "host"
    environment:
      - PYTHONUNBUFFERED=1
    command: bash -c "python src/data_preparation.py && tail -f /dev/null"
